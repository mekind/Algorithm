#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int testcase;
ll N, ans;
vector<ll> v;
ll ans;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    cin >> N;

    for (ll i = 0LL; i <= N; i++) {
        ll tmp;
        v.push_back(tmp);
    }

    cout << ans;

    return 0;
}